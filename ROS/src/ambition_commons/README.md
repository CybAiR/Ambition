# ambition commons

## launchfile
- `description` - robot description for simulation
- `motor_control` -  launches `md_node_launch` and runs `motor_control`
- `teleop_vel` - runs `joy_node` and `teleop_node` for controller teleoperation 

## config
- `motor_control` - parameter file loaded by `motor_control.launch.py`